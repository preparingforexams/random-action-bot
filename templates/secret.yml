apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  namespace: {{ .Values.namespace }}
data:
  BOT_TOKEN: "{{ .Values.secret.botToken }}"
  API_NINJAS_KEY: "{{ .Values.secret.apiNinjasKey }}"
